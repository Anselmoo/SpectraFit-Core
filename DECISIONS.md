# Design Decisions — spectrafit-core

This file is intentionally not published to the public GitHub mirror — it contains internal AI-collaboration planning and architecture-decision detail kept private to the primary GitLab repository. This stub exists only so the repository's required-file layout check (`rrt folder check`) passes on the public snapshot.
