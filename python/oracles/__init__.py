"""Test infrastructure: scenarios, synthetic data, oracle math, V&V harness.

Imported by bench/engine.py (at run time) and by pytest (at collection time).
Never imports from extras/bench/ except `contract` (the wire-format).
"""
